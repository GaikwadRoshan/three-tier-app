apiVersion: v1
kind: service
metadata:
  name: frontend-service
  namespace: three-tier
spec:
  selector:
    app: frontend

  ports:
    protocol: TCP
    port: 3000
    targetport: 3000
  type: ClusterIP